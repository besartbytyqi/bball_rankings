# Copy to `.env.local` (gitignored) and adjust if your NBA API is not on 8000.
# Example: another app uses 8000 — run uvicorn on 8002 and set:
# VITE_API_PROXY_TARGET=http://127.0.0.1:8002

VITE_API_PROXY_TARGET=http://127.0.0.1:8000
